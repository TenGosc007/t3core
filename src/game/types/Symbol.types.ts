/** A single player symbol (e.g. `'O'` or `'X'`). */
export type PlayerSymbol = string;

/** Tuple of symbols for the two players, `[firstPlayer, secondPlayer]`. */
export type PlayerSymbols = readonly [PlayerSymbol, PlayerSymbol];
